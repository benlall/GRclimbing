{% extends 'base.html.twig' %}

{% block body %}
    {% include 'default/header.html.twig' %}
    {% include 'default/nav.html.twig' %}

    <div class="container rounded">
        <div class="row justify-content-center">
            <section class="col-lg-9 text-center">
                <h2 class="mt-3 mb-3 h1-back">Mon profil</h2>

                <table class="table w-75 m-auto">
                    <tr>
                        <th>Email</th>
                        <td>{{ user.email }}</td>
                    </tr>
                    <tr>
                        <th>Login</th>
                        <td>{{ user.fullname }}</td>
                    </tr>
                </table>

                <a class="btn btn-secondary d-block w-50 mt-3 mx-auto" href="{{ path('edit_climber', { 'id': user.id }) }}">modifier mon identité</a>

                <a class="btn btn-secondary d-block w-50 my-3 mx-auto" href="{{ path('edit_password_climber', { 'id': user.id }) }}">changer mon mot de passe</a>

            </section>

            {% include 'default/other.html.twig' %}
        </div>
    </div>

    {% include 'default/footer.html.twig' %}

{% endblock %}

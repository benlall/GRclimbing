{% extends 'admin/base_admin.html.twig' %}

{% block section %}

<h1>Listes des news</h1>

<table class="table">
    <thead>
        <tr>
            <th>Titre</th>
            <th>Image du titre</th>
            <th>Contenu de l'article</th>
            <th>Date de rédaction</th>
            <th>Article public</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for post in posts %}
        <tr>
            <td>{{ post.title }}</td>
            <td><img src="{{ post.imgTitle }}" alt="image de la news"/></td>
            <td>{{ post.content }}</td>
            <td>{{ post.createdAt|date('d-m-Y') }}</td>
            <td>{% if post.public %}Yes{% else %}No{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('post_show', { 'id': post.id }) }}">Voir</a>
                    </li>
                    <li>
                        <a href="{{ path('post_edit', { 'id': post.id }) }}">Modifier</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('post_new') }}">Créer une nouvelle news</a>
    </li>
</ul>

{% endblock %}
